/*    Creates new artboard and adds additional readable properties        @return {Artboard} - Modified Illlustrator Object*/#include "AddReadableArtboardRect.jsx"var CreateArtboard = function(data) {        var doc = app.activeDocument;         if (typeof data == 'undefined') {        var data = {};    }        if (! data.hasOwnProperty('name')) { data['name'] = 'Zeichenfläche'; }    if (! data.hasOwnProperty('x')) { data['x'] = 0; }    if (! data.hasOwnProperty('y')) { data['y'] = 0; }    if (! data.hasOwnProperty('width')) { data['width'] = Millimeter(100); }    if (! data.hasOwnProperty('height')) { data['height'] = Millimeter(100); }        var artboard = doc.artboards.add([        data.x, // x1 or. x-top        data.y , // y1 or y-top         data.x + data.width, // x2 or x-bottom        data.y - data.height // y2 or y-bottom    ]);        artboard.name = data.name;        return new AddReadableArtboardRect(artboard);}
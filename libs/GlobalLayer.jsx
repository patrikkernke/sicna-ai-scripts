/*    Returns or creates a layer by given name        @param {string} name        @return {Layer} - Illlustrator Layer Object*/var GlobalLayer = function(name) {            var doc = app.activeDocument;        try {                // check if layer exist ...        var layer = doc.layers.getByName(name);        layer.locked = false;            } catch (error) {                // ... or create a new one         var layer = doc.layers.add();         layer.name = name;             }    return layer;} 
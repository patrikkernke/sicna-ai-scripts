/*    Returns active artboard and adds additional readable properties        @return {Artboard} - Modified Illlustrator Object*/#include "AddReadableArtboardRect.jsx"var ActiveArtboard = function() {        var doc = app.activeDocument;        var activeIndex = doc.artboards.getActiveArtboardIndex();    var artboard = doc.artboards[activeIndex];        return new AddReadableArtboardRect(artboard);}
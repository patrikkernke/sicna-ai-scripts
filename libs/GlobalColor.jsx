/*    Returns or creates a color by given name        @param {string} name    @param {array} colorValue (cyan, magenta, yellow, black)        @return {Swatch} - Illlustrator Swatch Object*/var GlobalColor = function(name, colorValue) {        var doc = app.activeDocument;        try {                var color = doc.spots.getByName(name);            } catch (error) {                var color = doc.spots.add();            var cmyk = new CMYKColor();        cmyk.cyan = colorValue[0];        cmyk.magenta = colorValue[1];        cmyk.yellow = colorValue[2];        cmyk.black = colorValue[3];            color.name = name;        color.colorType = ColorModel.PROCESS;        color.color = cmyk;            }    return color;}